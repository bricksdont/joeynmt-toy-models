# this file is adapted from:
# https://github.com/joeynmt/joeynmt/blob/master/configs/iwslt14_deen_bpe.yaml

name: "rnn"

data:
    src: "en"
    trg: "de"
    train: "data/train.bpe"
    dev:   "data/dev.bpe"
    test:  "data/test.bpe"
    level: "bpe"
    lowercase: False
    max_sent_length: 80
    src_vocab: "shared_models/vocab.txt"
    trg_vocab: "shared_models/vocab.txt"

testing:
    beam_size: 5
    alpha: 1.0

training:
    optimizer: "adam"
    normalization: "tokens"
    learning_rate: 0.0002
    batch_size: 80
    scheduling: "plateau"
    patience: 8
    weight_decay: 0.0
    decrease_factor: 0.7
    early_stopping_metric: "ppl"
    epochs: 100
    validation_freq: 2000
    logging_freq: 500
    eval_metric: "bleu"
    model_dir: "models/wmt17_ende_rnn_attlast"
    overwrite: False
    shuffle: True
    use_cuda: False
    max_output_length: 80
    print_valid_sents: [0, 1, 2, 3, 4]

model:
    tied_embeddings: True
    tied_softmax: True
    encoder:
        rnn_type: "gru"
        embeddings:
            embedding_dim: 512
            scale: False
        hidden_size: 512
        bidirectional: True
        dropout: 0.0
        num_layers: 1
    decoder:
        rnn_type: "gru"
        embeddings:
            embedding_dim: 512
            scale: False
        emb_scale: False
        hidden_size: 512
        dropout: 0.0
        hidden_dropout: 0.2
        num_layers: 1
        input_feeding: True
        init_hidden: "bridge"
        attention: "bahdanau"
